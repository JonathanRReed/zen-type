---
import "../styles/globals.css";
import QuoteTyper from "../components/QuoteTyper";
import StatsBar from "../components/StatsBar";
import PauseMenu from "../components/PauseMenu";
import HelpSheet from "../components/HelpSheet";
import AboutOverlay from "../components/AboutOverlay";
import ArchiveOverlay from "../components/ArchiveOverlay";
import SiteFooter from "../components/SiteFooter";
import SiteHeader from "../components/SiteHeader";
import ErrorBoundary from "../components/ErrorBoundary";
import SEO from "../components/SEO.astro";
import SpotlightLayer from "../components/SpotlightLayer";
import { loadQuotes, getRandomQuote } from "../utils/quotes";

const quotes = await loadQuotes();
const randomQuote = getRandomQuote(quotes);
const siteUrl = "https://zentype.jonathanrreed.com";
const homeUrl = `${siteUrl}/`;

const webApplicationJsonLd = {
	"@context": "https://schema.org",
	"@type": "WebApplication",
	"@id": `${homeUrl}#webapp`,
	name: "Zen Typer",
	alternateName: "ZenType",
	url: homeUrl,
	description:
		"A free mindful typing test and typing practice app. Type curated Zen quotes with live WPM and accuracy, or switch to a free-flow writing mode. No account, and sessions stay in the browser.",
	applicationCategory: "EducationalApplication",
	applicationSubCategory: "Typing test",
	operatingSystem: "Any device with a modern web browser",
	browserRequirements: "Requires JavaScript.",
	inLanguage: "en-US",
	isAccessibleForFree: true,
	offers: {
		"@type": "Offer",
		price: "0",
		priceCurrency: "USD",
		availability: "https://schema.org/InStock",
	},
	featureList: [
		"Quote typing test with live WPM and accuracy",
		"Free-flow Zen Mode for open writing practice",
		"Session stats, streaks, and drafts stored in the browser",
		"Keyboard-first controls with reduced-motion support",
	],
	creator: { "@id": `${homeUrl}#creator` },
	publisher: { "@id": `${homeUrl}#creator` },
	isPartOf: { "@id": `${homeUrl}#website` },
};
---

<!doctype html>
<html lang="en">
	<head>
		<SEO
			title="Mindful Typing Test and Practice for Focus | Zen Typer"
			description="Zen Typer is a free mindful typing test with Zen quotes, live WPM and accuracy, and a free-flow writing mode. No account, and sessions stay in your browser."
			canonical={homeUrl}
			jsonLd={webApplicationJsonLd}
			keywords={[
				"typing test",
				"mindful typing test",
				"typing practice",
				"typing trainer",
				"WPM test",
				"typing accuracy",
			]}
			breadcrumbItems={[
				{ name: "Mindful typing test", url: homeUrl },
			]}
		/>
		<!-- Developer Console Easter Egg -->
		<script>
			(function () {
				if (typeof window === "undefined") return;

				const ascii = `
    ___  ________  ________     
   |\\  \\|\\   __  \\|\\   __  \\    
   \\ \\  \\ \\  \\|\\  \\ \\  \\|\\  \\   
 __ \\ \\  \\ \\   _  _\\ \\   _  _\\  
|\\  \\\\_\\  \\ \\  \\\\  \\\\ \\  \\\\  \\|
\\ \\________\\ \\__\\\\ _\\\\ \\__\\\\ _\\
 \\|________|\\|__|\\|__|\\|__|\\|__|
				`;

				console.log(ascii);
				console.log("Hey there. Interested in code?");
				console.log(
					"Check out my GitHub: https://github.com/JonathanRReed",
				);
				console.log("Most of my sites repos are open source.");
			})();
		</script>
	</head>
	<body>
		<ErrorBoundary>
			<a
				href="#quote-app"
				class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-iris focus:text-base focus:rounded-lg focus:shadow-lg"
			>
				Skip to main content
			</a>
			<main
				id="quote-app"
				class="relative w-full h-screen overflow-hidden stats-cq-host"
			>
				<h1 class="sr-only">Mindful Typing Test and Practice</h1>
				<SiteHeader client:only="react" mode="quote" />
				<div
					class="pointer-events-none fixed inset-0 z-0 theme-layer"
				>
					<div class="forest-rays" aria-hidden="true"></div>
					<div class="ocean-lights" aria-hidden="true">
						<div class="stars-near" aria-hidden="true"></div>
						<div class="stars-far" aria-hidden="true"></div>
					</div>
				</div>
				<SpotlightLayer client:only="react" transition:persist="spotlight" />

				<div
					class="relative z-10 flex w-full items-center justify-center px-6 h-full"
				>
					<div class="w-full max-w-4xl">
						<QuoteTyper
							client:only="react"
							quote={randomQuote.text}
							author={randomQuote.author}
							onComplete={(summary) => {
								const completeEvent = new CustomEvent(
									"quoteComplete",
									{ detail: summary },
								);
								window.dispatchEvent(completeEvent);
							}}
						/>
						<!--
							First-paint stand-in for the client:only typing surface above.
							QuoteTyper renders nothing on the server, so without this the
							viewport stays blank until React hydrates and LCP is pushed
							behind the whole island bundle. This mirrors the hydrated card,
							carries the same build-time quote, and is retired by CSS the
							moment the island drops its `ssr` flag (see .quote-boot in
							globals.css). Must stay the element directly after the island.
						-->
						<div
							class="quote-boot absolute inset-0 flex flex-col items-center justify-center px-12 pt-12 pb-24"
						>
							<div class="w-full max-w-4xl">
								<div
									class="glass quote-card rounded-2xl px-8 pt-8 pb-7 mb-6"
								>
									<p
										class="quote-boot-text font-mono text-2xl leading-relaxed"
									>
										{randomQuote.text}
									</p>
									<p class="text-right text-muted text-lg mt-5">
										<span aria-hidden="true">—</span>
										{randomQuote.author}
									</p>
									<div class="quote-progress-track mt-6">
										<div
											class="quote-progress-fill"
											style="width: 0%"
										>
										</div>
									</div>
								</div>
							</div>
						</div>
					</div>
				</div>

				<StatsBar
					client:only="react"
					mode="quote"
					visible={true}
					metrics={{}}
				/>

				<PauseMenu
					client:only="react"
					mode="quote"
					onReset={() => {
						window.dispatchEvent(new CustomEvent("newQuote"));
					}}
					data-pause-menu
				/>

				<HelpSheet client:only="react" isOpen={false} onClose={() => {}} />
				<ArchiveOverlay client:only="react" />
				<AboutOverlay client:only="react" />
				<SiteFooter client:only="react" />
			</main>
		</ErrorBoundary>

		<!--
			`relative z-10` is load-bearing, not decoration. `.theme-layer` is
			`fixed inset-0 z-0` with an opaque theme gradient, so a static
			(z-index: auto) section paints in the in-flow background phase —
			underneath it — and this copy would be readable by crawlers but
			invisible to humans. z-10 matches the app's own content wrapper and
			puts this section above the theme layer in the root stacking context.

			pb-40 (was pb-28) is the same concern: the fixed `.stats-bar` sits at
			z-40 and, at max scroll, clipped the last 34px of the tallest column.
			160px of bottom padding lets the final row clear it.
		-->
		<section
			class="relative z-10 mx-auto max-w-5xl px-6 pb-40 pt-16 seo-body"
			aria-labelledby="home-seo-heading"
		>
			<div
				class="grid gap-10 border-t border-iris/15 pt-10 md:grid-cols-[1.15fr_0.85fr]"
			>
				<div class="space-y-4">
					<p class="text-sm font-medium text-foam">Typing practice</p>
					<h2
						id="home-seo-heading"
						class="max-w-2xl text-3xl font-semibold text-text md:text-4xl"
					>
						A mindful typing test for focus, speed, and accuracy.
					</h2>
					<p class="max-w-2xl leading-7 seo-body md:text-lg">
						Zen Typer combines a free typing test with quiet practice
						sessions, real-time WPM, accuracy feedback, and a
						free-flow writing mode for people who want typing practice
						without a noisy game layer.
					</p>
				</div>
				<div class="space-y-5 text-sm leading-6">
					<div>
						<h3 class="font-semibold text-text">Practice options</h3>
						<p>
							Start with the quote typing test for measured speed and
							accuracy, then switch to Zen Mode when you want a longer
							focused writing session.
						</p>
					</div>
					<div>
						<h3 class="font-semibold text-text">How the session feels</h3>
						<p>
							The interface favors quiet feedback over pressure. You can
							watch pace, accuracy, and completion without leaving the
							keyboard, then move into open writing when the measured
							drill has done its job. It is useful for short warmups,
							recovery breaks, and longer focus blocks where rhythm
							matters more than a public score.
						</p>
						<p>
							Because the page keeps the exercise narrow, it is useful
							before writing, coding, journaling, or any task where a few
							minutes of steady hands can make the next block feel easier.
						</p>
					</div>
					<div class="flex flex-wrap gap-4">
						<a class="text-foam transition-colors hover:text-text" href="/quote/">
							Quote typing test
						</a>
						<a class="text-foam transition-colors hover:text-text" href="/zen/">
							Free-flow typing practice
						</a>
					</div>
				</div>
				<div class="md:col-span-2 grid gap-6 border-t border-iris/15 pt-8 text-sm leading-6 md:grid-cols-3">
					<div>
						<h3 class="font-semibold text-text">How the test works</h3>
						<p>
							A Zen quote is shown one character at a time. The timer
							starts on your first keystroke and stops on the final
							character, so reading the quote first costs you nothing.
							Backspace rewinds to the last mistake instead of one
							character at a time.
						</p>
					</div>
					<div>
						<h3 class="font-semibold text-text">
							How WPM and accuracy are calculated
						</h3>
						<p>
							Words per minute is your correct characters divided by five,
							then divided by the elapsed minutes, using the standard
							five-character word. Accuracy is correct characters divided
							by every character you typed. Mistakes are grouped as slips,
							skips, and extras so you can see the pattern, not just the
							score.
						</p>
					</div>
					<div>
						<h3 class="font-semibold text-text">What is stored</h3>
						<p>
							Settings, session stats, streaks, archived sessions, and Zen
							Mode drafts are written to your browser's local storage and
							IndexedDB. There is no account and no sync, so clearing site
							data for this domain removes them. See the
							<a class="text-foam transition-colors hover:text-text" href="/privacy/">privacy notice</a>
							for the full detail.
						</p>
					</div>
				</div>
				<div class="md:col-span-2 grid gap-6 border-t border-iris/15 pt-8 text-sm leading-6 md:grid-cols-3">
					<div>
						<h3 class="font-semibold text-text">Private by default</h3>
						<p>
							Zen Typer works without accounts. Preferences and local
							session details stay in browser storage so a quick visit can
							still feel personal without sending drafts to a writing
							service.
						</p>
					</div>
					<div>
						<h3 class="font-semibold text-text">Accessible controls</h3>
						<p>
							The app keeps keyboard navigation, visible focus states,
							reduced-motion support, and descriptive control names in the
							core workflow so the main experience is not pointer-only.
						</p>
					</div>
					<div>
						<h3 class="font-semibold text-text">Project details</h3>
						<p>
							Read more on the <a class="text-foam transition-colors hover:text-text" href="/about/">about page</a>,
							send feedback through <a class="text-foam transition-colors hover:text-text" href="/contact/">contact</a>,
							or review the <a class="text-foam transition-colors hover:text-text" href="/privacy/">privacy notice</a>.
						</p>
					</div>
				</div>
			</div>
		</section>

		<script>
			import { navigate } from "astro:transitions/client";

			let isPaused = false;

			/** @param {KeyboardEvent} e */
			const handleKeys = (e) => {
				const ae = document.activeElement;
				// Tab switches mode only from the typing surface itself. The old
				// check was "not in any input", which never matched (the hidden
				// typing input always holds focus) so the documented shortcut
				// never fired, and it would have hijacked Tab everywhere else on
				// the page. Shift+Tab is deliberately left alone so keyboard
				// users can always step back out to the header.
				const onTypingSurface = !!(
					ae && ae.hasAttribute && ae.hasAttribute("data-typing-surface")
				);

				if (
					onTypingSurface &&
					e.key === "Tab" &&
					!e.ctrlKey &&
					!e.metaKey &&
					!e.shiftKey
				) {
					e.preventDefault();
					navigate("/zen/");
				}

				if (e.key === "Escape") {
					e.preventDefault();
					isPaused = !isPaused;
					window.dispatchEvent(
						new CustomEvent("togglePause", { detail: isPaused }),
					);
				}
			};

			// Attach once; ClientRouter preserves listeners via persist markers.
			document.addEventListener("keydown", handleKeys);
			document.addEventListener("astro:before-swap", () => {
				document.removeEventListener("keydown", handleKeys);
			});
			document.addEventListener("astro:after-swap", () => {
				document.addEventListener("keydown", handleKeys);
			});
		</script>
	</body>
</html>
